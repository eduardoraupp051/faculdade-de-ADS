// 1 Crie uma variável chamada idade e atribua a ela o valor 35 e exiba o seu valor.

int idade = 35;
Console.WriteLine(idade);

//Crie uma variável chamada nome e atribua o valor "Maria" e exiba o seu valor.
string nome = "Maria";
Console.WriteLine(nome);


// Crie uma variável chamada altura e atribua a ela o valor 3.45.

float altura = 3.45f;
Console.WriteLine(altura);


// Crie uma constante chamada ano e atribua a ela o valor 12 e exiba o seu valor

const int ano = 12;
Console.WriteLine(ano);

// Declare uma variável nota do tipo double como sendo do tipo Nullable type e atribua o valor 7.80 e exiba o seu valor

double? nota = 7.80;
Console.WriteLine("a nota é " + nota);


// Quais as diferenças entre os tipos por valor e os tipos por referência ? tipo por valor armazena em memoria criando uma copia e o tipo de referencia apenas faz referencia ao valor

// O que é um nullable type e qual a sua utilidade ? serve para que a variavel possa ficar sem receber valor ou o famoso null

// O que é Camel Case ? Dê um exemplo de sua aplicação. a primeira palavra fica em minuscula e o restante em maiuscula exemplo: nomeDoPaciente frequentemente usada em variaveis

// O que é Pascal Case ? Dê um exemplo de sua aplicação. A primeira palavara da variavel tem a letra  maiscula e o restante segue tendo a primeira pavara em maiuscula tambem RetentorDeBengala frenquentemente usada em nome de classes

// Declare duas variáveis x e y como sendo do tipo int e atribua a ela os valores 77 e 66 e a seguir imprima o valor da soma de x com y. (Use o operador aritmético + para realizar a operação de soma)
int x = 77;
int y = 66;
int soma = x + y;
Console.WriteLine(soma);


// Quais os valores padrões dos tipos de dados bool, char, int, double, float, decimal e string.